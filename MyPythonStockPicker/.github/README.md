This `.github/` directory is used to store repository-level GitHub files.

Place your `copilot-instruction.md` here so the assistant can read it and follow your instructions.

To copy the file from your local Downloads folder, run the script `scripts/install_copilot_from_downloads.sh` from the repository root.
